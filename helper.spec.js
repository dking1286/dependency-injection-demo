/*
  After this require statement is invoked, aany subsequent
  files will be transpiled on the fly by babel. This is what allows us to
  use es6 import syntax in dependency_injection.js
*/
require('babel-register')();
